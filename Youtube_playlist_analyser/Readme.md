# Fetch a youtube playlist and show its videos name and duration 

# Module used
npm i pdfkit
npm i puppeteer

# Usage
install puppeteer
install pdfkit
paste the playlist link in script
now run main.js